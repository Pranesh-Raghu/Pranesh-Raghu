name: 🧩 Update GitHub Stats

on:
  schedule:
    - cron: "0 1 * * *"  # runs daily at 6:30 AM IST
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Generate Stats with Token (this forces update)
        run: echo "Triggered stats refresh with token."
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
